# Side projects, separate repo
claude-code-digest/
sqli-ai-taskforce/
wiki-data/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~

# Large binary outputs (slide-gen outputs tracked in slide-gen repo)
*.pptx
*.xlsx
*.pdf

# Session analysis findings — local only, not versioned
.tasks/sessions-findings/*
!.tasks/sessions-findings/.gitkeep

# Session removal logs — local only, not versioned
.logs/sessions-removed/*
!.logs/sessions-removed/.gitkeep
