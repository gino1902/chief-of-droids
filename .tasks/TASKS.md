<!-- version: 1.40 | author: chief-of-droids workspace | last_updated: 2026-05-04 -->

# TASKS.md — workspace

> Workspace-level task tracker. Scope: cross-repo infrastructure, shared skills library,
> workspace-wide maintenance.
> Managed by Claude (proposed) + approved by you.
> Triggers: `show tasks` | `next task` | `start task TASK-XXX` | `done task TASK-XXX` | `add task`
> Done entries are archived to `.tasks/archive.md` — use `show archive` to retrieve history.

---

## 🔴 Backlog

- TASK-009: Refine project-bootstrapping skill post first-run | target: workspace/skills/project-bootstrapping/ | scope: update SKILL.md, project-interview.md, challenge-rules.md, output-templates.md based on TASK-008 findings; bump skill version to 1.2 | origin: session:2026-03-19 "Setting up Claude framework context"
- TASK-018: Design and implement a repeatable skill testing approach using official tooling | target: workspace/skills/ | scope: originated from analyzing-business-cases skill remediation (2026-03-23) — trigger gaps and misroutings only surfaced after 5 iterative challenge rounds; current approach is manual, ad hoc, session-dependent, non-repeatable; selected approach: Option B — Desktop authors the test harness spec; Claude Code executes the eval + optimization loop using anthropics/skill-creator (official Anthropic meta-skill); tool stack: (1) skill-creator — test case generation (evals/evals.json schema: id, prompt, expected_output, assertions), eval execution (Executor + Grader + Comparator + Analyzer subagents), description optimization loop (60/40 train/test split, 3 runs per query, up to 5 iterations, selects by test score to avoid overfitting); (2) hqhq1025/skill-optimizer — post-production session analysis (trigger rate, undertrigger detection, user reaction, workflow completion, static quality, token economics); routing adaptation required: skill-creator uses frontmatter-based trigger detection (should_trigger boolean) — this workspace routes via HOW-TO-TRIGGER.md; wrapper needed to substitute HOW-TO-TRIGGER.md routing check for frontmatter scanning in the trigger eval queries; deliverable: (1) per-skill tests/ directory structure with evals/evals.json, (2) routing-adapter note in workspace CLAUDE.md or a per-skill tests/README.md, (3) first live run on one skill (TBD); two-layer model: skill-creator covers pre-production authoring loop; skill-optimizer covers post-production feedback loop | origin: session:2026-03-23 "Actions history review" | updated: session:2026-04-18 "Claude Code agent calling from Desktop — skill-creator + skill-optimizer research"
- TASK-026: Create docs/system-prompt-changelog.md | target: workspace/docs/system-prompt-changelog.md | scope: 4 system prompt changes captured in 2026-03-28 session findings file under Net-New Content → System Prompt Changes; promote verbatim: initial build (2026-03-19), full overhaul (2026-03-26), file-edit rule (2026-03-26), git rules (2026-03-20); this is the highest-risk unrecorded gap — no recovery point without it | origin: findings:2026-03-28 "Workspace session analysis run 1"
- TASK-027: Add Mermaid process diagram to docs/how-to-create-a-skill.md | target: workspace/docs/how-to-create-a-skill.md | scope: document exists; add a Mermaid diagram illustrating the skill authoring process flow | origin: session:2026-03-28 "Managing sessions build"
- TASK-028: Refactor close-session workflow — add completed tasks step | target: chief-of-droids project system prompt \<rules\> | scope: the close-session / git commit workflow currently stages and commits files but does not include a step to add completed work to TASKS.md; refactor to insert a mandatory "add tasks completed" step before staging — ensuring task state is always committed alongside the work it records; encode as a system prompt rule (predecessor `prompt-maintenance.md` location was deprecated 2026-05-03) | origin: session:2026-03-28 "Managing sessions build" | updated: session:2026-05-03 "Dual-environment-handoff migration framing"
- TASK-031: Populate MCP Evaluation Table with scores for 7 community MCPs | target: .tasks/backlog/MCP Evaluation Table.md | scope: source catalog is https://github.com/modelcontextprotocol/servers; evaluate 7 listed MCPs (Everything, Fetch, Filesystem, Git, Memory, Sequential Thinking, Time) against 5 scoring dimensions; fill Address, Catalog, Guide, Repo columns and record final score per entry | origin: session:2026-03-28 "Managing sessions build"
- TASK-032: Design session-based MCP recommendation workflow using managing-sessions output | target: TBD (skill or doc) | scope: user story from MCP Evaluation Table.md — scan sessions, surface candidate MCPs from community catalog; design workflow with options evaluated; implementation is a separate task | origin: session:2026-03-28 "Managing sessions build"
- TASK-035: Author skill `recording-decisions` | target: workspace/skills/recording-decisions/ | scope: surfaced by recommend-skills live run (2026-03-28, High confidence, 3 sessions); captures ADRs, design decisions, and evaluation outcomes to disk; prevents unrecorded decisions that recur across sessions; reference sources: DAIR.AI prompt engineering guide + Anthropic Skills Repo (pattern available); trigger on: "record decision", "this is an ADR", "log this decision", "capture outcome"; writes structured decision entries to docs/decisions/ or inline ADR files | origin: skill:recommend-skills "Recommend skills workflow"
- TASK-036: Author skill `promoting-findings` | target: workspace/skills/promoting-findings/ | scope: surfaced by recommend-skills live run (2026-03-28, High confidence, 4 sessions); promotes not-on-disk content from session findings files to canonical docs (mcp-tool-quirks.md, system-prompt-changelog.md, and similar); no external source — model on managing-sessions output workflow (session-log-schema.md pattern); reads findings file, identifies not-on-disk entries, promotes verbatim to target canonical doc, updates Promoted to column | origin: skill:recommend-skills "Recommend skills workflow"
- TASK-037: Author skill `enforcing-tool-constraints` | target: workspace/skills/enforcing-tool-constraints/ | scope: surfaced by recommend-skills live run (2026-03-28, High confidence, 2 sessions); encodes known tool failure modes as standing constraints Claude reads at session start; prevents str_replace recurrence and similar; reference source: Anthropic Skills Repo (constraint pattern from production skills); covers str_replace silent failure, filesystem:write_file silent failure, read_multiple_files bootstrap issue, git_diff_unstaged bypass | origin: skill:recommend-skills "Recommend skills workflow"
- TASK-051: Migrate TASKS.md storage to SQLite MCP | target: workspace/skills/managing-tasks/ + workspace/.tasks/ | scope: replace Filesystem read/write of TASKS.md with SQLite-backed task storage; prerequisite: confirm SQLite MCP (modelcontextprotocol/servers) is installable in macOS environment; evaluate schema migration from flat file to relational rows; deliverable is implementation replacing Phase A (archive.md) with a proper DB-backed solution; implementation follows TASK-048 Option B design | origin: session:2026-03-31 "Refactor managing-tasks — .tasks/ migration"
- TASK-053: Align system prompt fetch-verification rule with Desktop tool name | target: chief-of-droids project system prompt \<rules\> block | scope: current rule reads "never cite an external source unless its full content has been fetched and verified via web_fetch" — `web_fetch` is the claude.ai runtime tool name; in Claude Desktop sessions the equivalent tool is `fetch` (mcp-server-fetch); update rule to reference the correct tool per session context, or generalise to "fetch and verify via available fetch tool (web_fetch in claude.ai / fetch in Claude Desktop)"; no behavioural change, label and clarity only | origin: session:2026-04-03 "mcp-server-fetch evaluation and install" | updated: session:2026-05-03 "Dual-environment-handoff migration framing"
- TASK-054: Backfill references/qa-checklist.md for all remaining skills | target: workspace/skills/ | scope: creating-skills and executing-tasks received qa-checklist.md this session (2026-04-06); remaining skills without a QA checklist: managing-tasks, writing-docs, managing-sessions, architecting-data-platforms, reviewing-tech-claims, project-bootstrapping, analyzing-business-cases; for each: author qa-checklist.md using references/qa-template.md (created this session), apply placement decision rule, verify minimum coverage requirements, write to references/; assessment-checklist.md v1.6 now flags absence as Major — all remaining skills will fail critique on this item until backfilled | origin: session:2026-04-06 "QA checklist Level 3 pattern"
- TASK-055: Critique and refactor executing-tasks skill | target: workspace/skills/executing-tasks/SKILL.md | scope: full assessment-checklist.md critique run against v1.23; findings challenged and resolved: W1 (Blocking — TASKS.md tool-error handling at Step 1), W2 (Major — workflow state declaration with context-held artifacts and recovery instruction), W4 (Major — QA suite rejection/revision path distinguishing criteria vs expansion objection), W5 (Minor — CLAUDE.md tool reference in Reference Files), W7 (Minor — classification fallback parenthetical in Step 3); W3 closed as non-issue (executing-tasks scope ends at test pass; managing-tasks hand-off is a user prompt, not a workflow step); F1 deferred as Minor (portability concern only); version bumped 1.23 → 1.24 | origin: session:2026-04-06 "Critique and refactor executing-tasks skill"
- TASK-069: Migrate mcp-server-fetch and mcp-server-git from uvx to uv tool install | target: claude_desktop_config.json | scope: replace uvx invocations with installed binaries to eliminate stdout install noise at Claude Desktop launch (root cause of orange MCP indicator — TASK-068); run `uv tool install mcp-server-fetch` and `uv tool install mcp-server-git`; update config command fields to absolute binary paths; trade-off accepted: manual `uv tool upgrade` required for updates vs auto-latest via uvx | origin: session:2026-04-17 "critique skill standardizing-artifacts"
- TASK-070: Build parallel-agent orchestrator artifact | target: workspace (artifact) | scope: implement a Claude Desktop artifact that fans out N concurrent Anthropic API calls via Promise.all, each call being an independent Claude instance; artifact takes a task decomposition as input (array of subtask prompts), fires them in parallel, collects and synthesises results; design decisions to resolve: (1) subtask prompt authoring UX — free-form input vs structured form, (2) result synthesis strategy — sequential display vs Claude-synthesised summary, (3) state persistence across iterations — in-memory only or artifact storage API, (4) error handling per subtask — fail-fast vs collect-all-then-report; prerequisite: TASK-069 resolved (clean MCP startup) is not a hard blocker but reduces noise during development | origin: session:2026-04-18 "Claude Code agent calling from Desktop" | updated: session:2026-05-03 "Dual-environment-handoff migration framing"
- TASK-071: Design and implement multi-session working model for concurrent Claude Desktop sessions | target: workspace (CLAUDE.md + skills/) | scope: the framework is single-session by design — no write coordination, no resource ownership, no session namespacing; two concurrent Desktop sessions on the same workspace produce working-tree collisions regardless of branch strategy (last filesystem:write_file wins before any git_add occurs); the correct model is one working tree per session, mapped to git worktrees, with explicit session init/merge-back protocols; this is the chief-of-droids equivalent of the standard multi-developer git workflow (separate clone per developer); design decisions to resolve: (1) session init protocol — how a new session creates a named worktree at a declared filesystem path (command sequence, naming convention e.g. worktrees/session-YYYY-MM-DD-N/); (2) MCP root scoping — how CLAUDE.md routing rules are scoped to the session's worktree root rather than the main workspace root; approach options: worktree-local CLAUDE.md override vs system prompt parameter vs session-scoped env variable; (3) shared asset strategy — `.tasks/TASKS.md`, `skills/`, `docs/` are shared across sessions; options: (a) replicate per worktree + merge-back, (b) treat as read-only within session + defer all writes to merge, (c) designate one session as writer for shared assets, others as read-only; (4) merge-back workflow — what happens at session close: branch merge strategy (rebase vs merge commit), conflict resolution ownership, who triggers the merge; (5) CLAUDE.md commit gate interaction — the mandatory "do you want to commit?" gate fires after every write_file; in a worktree model this gate should commit to the session branch, not main; verify gate behaviour is branch-aware; (6) session identity — whether sessions need a declared identity (session name, branch name) surfaced at bootstrap, and where that identity is stored (sentinel file, worktree name, CLAUDE.md override); constraints: git_push unavailable via MCP — push and merge remain manual Terminal operations; worktree creation requires a git command not yet in the MCP tool set — evaluate whether git-workspace MCP supports `git worktree add` or whether this must be a Terminal prerequisite step; deliverables: (a) docs/multi-session-model.md — ADR capturing design decisions and rationale, (b) updated session init protocol in workspace CLAUDE.md or system prompt, (c) merge-back checklist added to close-session workflow (see also TASK-028), (d) CLAUDE.md git section updated with worktree-aware commit gate behaviour; prerequisite assessment: confirm `git worktree add` availability in git-workspace MCP before designing the init protocol | origin: session:2026-04-18 "concurrent Claude Desktop sessions architecture" | updated: session:2026-05-03 "Dual-environment-handoff migration framing"
- TASK-076: Rewrite architecting-data-platforms description in directive pattern | target: workspace/skills/architecting-data-platforms/SKILL.md | scope: current description is passive (Variant A per Medium April 2026 650-trial study, ~37% activation when paired with hooks); rewrite using directive pattern: "<Domain> expert. ALWAYS invoke this skill when user asks about [trigger tokens]. Do not [alternative] directly — use this skill first."; trigger token list must include: ADR, architecture decision record, architecture requirements, data platform design, lakehouse, medallion, governance, observability, NFR; pairs with HOW-TO-TRIGGER.md v1.12 routing where ADR prompts trigger this skill + writing-docs together; reference: agentskills.io spec + Medium 650-trial study; without this rewrite, multi-skill activation for "write an ADR" prompts will underperform | origin: session:2026-04-29 "writing-docs repositioning + elevate role-alias overhaul" + commit:78e58074
- TASK-077: Rewrite analyzing-business-cases description in directive pattern | target: workspace/skills/analyzing-business-cases/SKILL.md | scope: same rationale as TASK-076 — Variant A passive descriptions activate at ~37% with hooks per Medium 650-trial study; rewrite using directive pattern with negative constraint; trigger token list must include: BDR, business decision record, use case, acceptance test, acceptance criteria, business requirements, requirements brief, framing; pairs with HOW-TO-TRIGGER.md v1.12 routing where these prompts trigger this skill + writing-docs together; existing triggers (build framing, challenge framing, assess-artifacts) preserved; reference: agentskills.io spec + Medium 650-trial study | origin: session:2026-04-29 "writing-docs repositioning + elevate role-alias overhaul" + commit:78e58074
- TASK-078: Migrate inline templates from deleted writing-docs/references/templates.md into owning authoring skills | target: workspace/skills/architecting-data-platforms/references/ + workspace/skills/analyzing-business-cases/references/ | scope: templates.md was deleted (tombstoned) in commit 78e58074; original 4-template content recoverable from git history at commit immediately preceding 78e58074; ADR template → architecting-data-platforms/references/template-adr.md; Requirements Brief template → analyzing-business-cases/references/template-requirements-brief.md; Runbook template → hold for future runbooks skill (no skill yet); Playbook template → hold for future playbooks skill (no skill yet); preserve template content verbatim from git history; update each authoring skill's SKILL.md Reference Files block to list its new template; update HOW-TO-TRIGGER.md only if templates change which routing applies; complete deletion of writing-docs/references/templates.md tombstone (`git rm`) after migration verified; blocked on TASK-076 + TASK-077 (need authoring skills' descriptions in place first) | origin: session:2026-04-29 "writing-docs repositioning + elevate role-alias overhaul" + commit:78e58074
- TASK-079: Propagate v2 role aliases to derived elevate-theme files | target: workspace/shared/elevate-theme/ | scope: tokens.json bumped to v2 in commit 78e58074 with new roles{} (tx1/tx2/tx3/bg1/bg2), text_role_constraints{}, typography{}, tables{}, contrast_pairs{}; derived files still hold v1.x palette names without role aliases: elevate.css, elevate-tokens.js, elevate-tailwind-v4.css, elevate-mermaid.md; regenerate each to add role-alias variables (--elevate-tx1, --elevate-bg1, etc) alongside existing palette variables; elevate-mermaid.md needs text color updated from #000000 to #0F0E2B (tx1) and background from #FFFFFF to #FFFAF0 (bg1) per new role assignments; README.md mermaid example already updated in 78e58074 — propagate to elevate-mermaid.md itself; verify against tokens.json as canonical source | origin: session:2026-04-29 "writing-docs repositioning + elevate role-alias overhaul" + commit:78e58074
- TASK-080: Update .docx generator script to inject settings-clrSchemeMapping.xml | target: TBD (workspace-level generator script — last instance was /home/claude/work/generate-v2.js for SQLI 2028 renders this session) | scope: settings-clrSchemeMapping.xml added in commit 78e58074 to shared/elevate-theme/; theme.md routing now requires injecting BOTH theme1.xml (into word/theme/theme1.xml) AND the <w:clrSchemeMapping> element (into word/settings.xml as a child of <w:settings>); current generator only injects theme1.xml; modify generator to: (1) read settings-clrSchemeMapping.xml, (2) parse out the <w:clrSchemeMapping> element, (3) insert as child of <w:settings> in word/settings.xml of the unpacked docx, (4) repack; reference: ECMA-376 §2.15.1.78 attribute values use long forms (light1/dark2/accent1, NOT lt1/dk2); validation test: produce a sample .docx and verify Word resolves heading colors correctly via the mapping (heading text should resolve to dk2/#0F0E2B via t1 mapping, page background to lt2/#FFFAF0 via bg1 mapping) | origin: session:2026-04-29 "writing-docs repositioning + elevate role-alias overhaul" + commit:78e58074
- TASK-081: Decide ownership of Architecture Requirements body template + qa file | target: workspace/skills/architecting-data-platforms/ OR workspace/skills/analyzing-business-cases/ | scope: template-architecture-requirements.md and qa-architecture-requirements.md marked deprecated in writing-docs/references/ in commit 78e58074 with note "ownership not yet decided"; decision needed: does Architecture Requirements authoring belong to architecting-data-platforms (technical/system view) or analyzing-business-cases (business requirements derivation)?; both skills could plausibly own it; once decided, migrate body template + qa file to chosen skill's references/, update that skill's SKILL.md Reference Files block to auto-load qa file when template is in use, remove deprecated files from writing-docs/references/; alternative split possible: requirements gathering (analyzing-business-cases) → architecture requirements derivation (architecting-data-platforms); blocked on TASK-076 + TASK-077 (directive description rewrites) since the owning skill needs trigger surface in place first | origin: session:2026-04-29 "writing-docs repositioning + elevate role-alias overhaul" + commit:78e58074
- TASK-083: Re-establish system prompt token-audit methodology under dual-environment architecture | target: workspace/docs/ (path TBD) | scope: prompt-maintenance.md was deprecated and removed in 2026-05-03 sweep; previous version covered single-environment audit pattern (4 steps: challenge as whole, measure tokens, verify behavioural arguments at activation scope, verify against Anthropic effective-context-engineering docs); under dual-environment architecture, "the system prompt" is no longer a single thing — Landspeeder has Claude Desktop project Custom Instructions, Sandcrawler has CLAUDE.md hierarchy with walk-up traversal and concatenation; new methodology must (1) clarify which environment's prompt each step audits, (2) account for CLAUDE.md-per-environment vs single-system-prompt model, (3) preserve the four-step pattern's core value (activation-scope reasoning, official-doc verification) while adapting to per-environment scope, (4) define trigger conditions per environment; reference predecessor: git history at commit immediately preceding 2026-05-03 deprecation | origin: session:2026-05-03 "Dual-environment-handoff migration framing"
- TASK-084: Encode HEAD-vs-working-tree drift check into File Edits discipline | target: workspace/CLAUDE.md `## File Edits` section + workspace/docs/mcp-tool-quirks.md | scope: surfaced 2026-05-04 during deprecation sweep — `filesystem:read_text_file` returned uncommitted working-tree content as if it were canonical in two separate cases (TASKS.md showed v1.38 when HEAD was v1.37; archive.md showed 3 entries when HEAD had 60); root cause: previous sessions wrote to working tree but never staged or committed; current session's read picked up that uncommitted state without any signal that it differed from HEAD; archive.md case was caught only by inspecting the staged diff before commit — without that gate, 57 historical task records would have been silently destroyed; required deliverables: (1) extend CLAUDE.md `## File Edits` rule from "Pattern: filesystem:read_text_file → filesystem:write_file (full rewrite)" to include a HEAD-drift check — proposed addition: "Before any write, when the file is part of a versioned write workflow, verify the read returned canonical content via `git_show HEAD:<path>` comparison or via post-write `git_diff_staged` inspection; any drift between read and HEAD = uncommitted work from a prior session = stop and reconcile before proceeding"; (2) add a new entry to docs/mcp-tool-quirks.md titled "filesystem:read_text_file returns uncommitted working-tree content silently" — covers symptom (read returns content, no warning of drift from HEAD), root cause (working tree may contain uncommitted changes from prior sessions), detection (compare read against `git_show HEAD:<path>`), recovery (`git_reset` if a write was already staged on the corrupted base, then re-write from HEAD content); (3) consider whether the rule should fire for every write or only for writes against versioned files in `.tasks/` and `skills/` (high-risk surfaces); (4) integrate with existing executing-tasks Step 7 inner-loop checklist if applicable; reference: this session's `chore(archive)` commit 6169d56 surfaced the issue | origin: session:2026-05-04 "Dual-environment-handoff migration framing — deprecation sweep"

---

## 🟡 In Progress

- TASK-008: First-run test of project-bootstrapping skill | target: workspace/skills/project-bootstrapping/ | scope: open Chief of Droids project, trigger `bootstrap project`, validate 7-section form renders, Phase 2 challenge fires, Phase 3 writes FRAMING.md → CLAUDE.md → TASKS.md → system prompt in sequence; record gaps for TASK-009 | origin: session:2026-03-19 "Setting up Claude framework context"

---

## ✅ Done

*Last updated: 2026-05-03 — TASK-010 and TASK-011 cancelled (deprecated sub-repos datawan/ and slide-gen/)*
