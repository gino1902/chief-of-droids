[project]
name = "use_case"
version = "0.0.1"
description = "Gold for the use_case business use case."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[dependency-groups]
dev = ["pytest", "pyspark", "chispa"]
