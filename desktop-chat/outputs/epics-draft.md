# EPICS - Draft version

## ENABLERS

- Construct pipeline CI/CD from PHP "extractor" to Databricks, so dev team test end-to-end deployment at the end of each sprint.

- Build Claude Code-DB framework so development team can orchestrate developments with control and predictability.

- Build ingestion pipeline for the data extracted from APP, whoz, smartrecruiterd, TBP, MSDynamics so data consummed by user meets quality requirements.

- Build Predictive Engines v1 so system make priorities recommendations to user for recruitment and upskilling.

## USER REQUIREMENTS

- Build data catalog so user and agents rely on trusted data with business semantics.

- Build Persona `<x>` components `<y, z, ...>` so user can define the right staffing and recruitment priority.