# EPICS - Draft version

## ENABLERS

- Setup development, test and production environnments, so proper risk control is ensured
- Construct CI/CD pipeline from PHP "extractor" to Databricks, so end-to-end deployment can be tested at the end of each sprint.
- Build Claude Code-DB framework so development can be AI orchestrated with control and predictability.
- Build ingestion pipeline from APP, whoz, smartrecruiterd, TBP, MSDynamics so data consummed by user so the provided data meets enterprise quality and security requirements.
- Build cost monitoring for compute and storage so cost ownership can be controlled.
- Build usage monitoring for ingestion and consuption so the cost can be user/agent traceable. 
- Build Predictive Engines v1 so system make priorities recommendations to user for recruitment and upskilling.

## USER REQUIREMENTS

- Users and agents will use data for catalog trustable and with business semantics.
- Persona `<x>` will use components `<y, z, ...>` so they can manage the right staffing and recruitment priorities.