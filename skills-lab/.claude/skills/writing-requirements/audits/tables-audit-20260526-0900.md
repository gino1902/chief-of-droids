AUDIT REPORT
Artifact: /Users/gilllesmourgues/Workspace/chief-of-droids/skills-lab/.claude/skills/writing-requirements/references/tables.md
Target Environment: claude-code (inferred — file path under .claude/skills/writing-requirements/references/; loaded by parent SKILL.md which declares claude-code)
Target Model: undeclared (parent SKILL.md declares claude-opus-4-7; this reference file does not inherit declaration)
Best-Practices Ref: 2026-05-17 snapshot
Date: 2026-05-26
Auditor: claude-opus-4-7

VIOLATIONS

STR-2 Structure and separation
 Severity: Major
 Finding: top-level components (DR — Data Requirements, TR — Transformation Requirements, Phase 4 hygiene checks) are separated only by markdown `##` headers — no XML tag boundaries.
 Location: "## DR — Data Requirements (T3) ... ## TR — Transformation Requirements (T4) ... ## Phase 4 hygiene checks (T3/T4)"
 Citation: best-practices snapshot, "Structure prompts with XML tags."
 Fix: wrap top-level components in named XML tags: `<dr-drafting>`, `<tr-drafting>`, `<phase-4-hygiene>`.

EX-1 Examples
 Severity: Major
 Finding: zero worked examples in the file. DR scope list, DR exclusion list, TR candidate signals (5 categories), TR exclusion list — all describe rules and rerouting decisions in prose without showing a single substrate sentence mapped to a DR-NNN or TR-NNN entry.
 Location: throughout — "DR scope — what qualifies", "DR exclusion list", "TR candidate signals", "TR exclusion list".
 Citation: best-practices snapshot, "Include 3–5 examples for best results."
 Fix: add 3–5 worked examples per non-trivial rule — for DR: substrate-with-schema → DR-NNN entry; substrate-without-schema → CON reroute; for TR: each candidate-signal category → worked TR-NNN entry; each exclusion category → reroute target identified.

EX-4 Demonstration where possible
 Severity: Major
 Finding: DR entry format and TR entry format are shown as templates but never instantiated. The "useful test" heuristics (fewer than two field rows; substrate names input + output but rule is implicit) are described but not demonstrated.
 Location: DR entry format block (`**DR-NNN** — <entity name>`); TR entry format block; both "A useful test:" paragraphs.
 Citation: best-practices snapshot, "Examples are one of the most reliable ways to steer Claude's output."
 Fix: add a complete worked DR-NNN entry showing a sample entity with 3–4 typed fields and lifecycle notes; add a complete worked TR-NNN entry showing a 3-row input→output table; add a worked example for each useful-test heuristic showing the borderline case and its reroute.

RSN-1 Reasoning context
 Severity: Major
 Finding: multiple non-obvious rules carry no Reason clause — DR exclusion list entries ("Free-form markdown files referenced as inputs or outputs — these are scoped via CON ..."); TR exclusion list entries ("Validation predicates with no output transformation — these are FR Validate-shape"); "A useful test:" heuristics.
 Location: DR exclusion list; TR exclusion list; both useful-test paragraphs.
 Citation: best-practices snapshot, "Add context to improve performance."
 Fix: append Reason clauses — DR-vs-CON reroute: "Reason: a markdown file without a typed schema is not a data entity; treating it as DR creates a contract for fields the substrate does not declare"; TR-vs-FR reroute: "Reason: TR is a deterministic input→output mapping; pure validation has no output transformation, so the contract surface is FR Validate-shape, not TR"; useful-test: "Reason: minimal column-typing signals the substrate did not author a data entity; surfacing it as DR over-claims schema authority."

OBL-3 Obligation language — hedging
 Severity: Minor
 Finding: hedging qualifier "probably" appears in a rule.
 Location: "if the table you would draw under `**DR-NNN**` has fewer than two field rows or the rows have no typed Constraints column entries, the entity is probably not a DR."
 Citation: in-house; aligned with "Be clear and direct".
 Fix: convert to deterministic — "if the table has fewer than two field rows OR the rows lack typed Constraints column entries, the entity is reclassified as CON and emits Warning."

EX-2 Examples — edge cases
 Severity: Minor
 Finding: no examples means no edge cases either; the "useful test" heuristics are described in prose only.
 Location: throughout.
 Citation: best-practices snapshot, "Diverse: cover edge cases."
 Fix: subsumed by EX-1 and EX-4 fixes — the added examples must include edge cases (entity that fits both DR and CON; transformation that fits both TR and FR).

RSN-3 Reasoning context — chain-of-thought
 Severity: Minor
 Finding: chain-of-thought behavior is not addressed.
 Location: no CoT directive present.
 Citation: best-practices snapshot, "Leverage thinking" + "Migrating away from prefilled responses."
 Fix: add an execution rule at the top of the file — "Reason internally when classifying substrate signals against DR vs CON and TR vs FR scope rules. Emit only DR-NNN and TR-NNN entries per format spec; do not include reasoning narration in output."

BRN-2 Branching and edge cases
 Severity: Minor
 Finding: edge cases not enumerated — substrate carrying a DR entity that also satisfies an IR-IN payload contract; substrate carrying a TR that also satisfies an FR Transform-shape ERR.
 Location: DR exclusion list; TR exclusion list.
 Citation: in-house.
 Fix: enumerate the cross-category cases — "An entity satisfying both DR and IR-IN is rendered under DR; IR-IN cross-links via `→ DR-NNN`"; "A transformation that produces an output also subject to FR Transform-shape ERR coverage renders under both: TR-NNN for the rule, ERR-NNN for the failure mode, cross-linked."

OUT-4 Positive framing
 Severity: Minor
 Finding: multiple negatives where positive form is unambiguous — "Do not derive DR from..."; "Do not derive TR from..."; "No RFC 2119 keywords and no EARS syntax inside DR tables"; "No RFC 2119 keywords and no EARS syntax inside TR tables".
 Location: DR exclusion list; TR exclusion list; entry-format rules.
 Citation: best-practices snapshot, "Tell Claude what to do instead of what not to do."
 Fix: convert to positive form — "Derive DR only when substrate names typed fields, constraints, or lifecycle"; "Derive TR only when substrate matches one of the candidate-signal categories"; "Use plain markdown table syntax inside DR and TR; RFC 2119 and EARS keywords appear only in their respective sections."

VER-1 Version declaration
 Severity: Minor
 Finding: target Claude model is not declared in the artifact.
 Location: file has no bottom metadata block.
 Citation: best-practices snapshot, "Prompting Claude Opus 4.7" + "Migration considerations."
 Fix: add a bottom metadata block including Target Model, Target Environment, Best-Practices Ref, Last Revised, Revision Source.

VER-2 Revision metadata
 Severity: Minor
 Finding: file lacks any bottom metadata block.
 Location: file ends at "TR entries describe transformations matching one of the candidate-signal categories | Warning if transformation has no matching category".
 Citation: in-house.
 Fix: append the complete metadata block per VER-1 fix, plus Version/Last Updated/Status fields.

SUMMARY
Total criteria: 41
Passed: 30
Failed: 11
 — Blocking: 0
 — Major: 4
 — Minor: 7
Risk level: High — do not deploy

| Field        | Value      |
|:-------------|:-----------|
| Version      | 1.0        |
| Last Updated | 2026-05-26 |
| Status       | Draft      |
